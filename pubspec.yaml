name: onepicker
description: Introducing our innovative data picker called OnePicker, a powerful tool that allows you to access a wide range of data effortlessly.
version: 0.0.5
homepage: https://github.com/udithperera-dev/onepicker

environment:
  sdk: ">=2.16.2 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.18.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0

topics:
  - onepicker
  - date-picker
  - date-range-picker
  - data-picker
  - picker